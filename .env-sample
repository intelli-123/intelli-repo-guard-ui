# .env.example - Rename this file to .env and fill in your actual credentials

# --- Google Gemini API Key ---
# Required for AI code analysis.
# Get one from: https://ai.google.dev/
GOOGLE_API_KEY=

# --- Private Repository Settings (Optional) ---
# Set to 'false' if scanning private repositories. Default is 'true' for public.
# If set to 'false', you MUST provide GIT_PROVIDER, GIT_USERNAME, and a token.
IS_PUBLIC_REPO="true"

# If IS_PUBLIC_REPO is "false", uncomment and configure the following:
# Specify your Git provider: "github", "gitlab", or "bitbucket"
# GIT_PROVIDER="github"

# Your Git username (e.g., your GitHub username)
# GIT_USERNAME="your-git-username"

# Your personal access token/app password for the specified provider
# GITHUB_TOKEN="ghp_YOUR_GITHUB_PAT_WITH_REPO_SCOPE"
# GITLAB_TOKEN="glpat-YOUR_GITLAB_PAT_WITH_READ_REPOSITORY_SCOPE"
# BITBUCKET_APP_PASSWORD="YOUR_BITBUCKET_APP_PASSWORD_WITH_REPOSITORY_READ_PERMISSION"

# For GitHub, PATs should have 'repo' scope for private repositories.
# For GitLab, PATs should have at least 'read_repository' scope.
# For Bitbucket, App Passwords need 'Repository Read' permission.